# Architecture

- `controller` handles HTTP exposure
- `service` contains business logic
- `dto` contains contract models

This template is intended to be extended into a real service with repositories,
security, observability, and persistence.
