# Architecture

- `contracts/` smart contracts
- `scripts/` deployment automation
- `test/` smart contract tests
- `web/` starter dashboard

Recommended extensions:
- add OpenZeppelin dependency
- integrate a frontend wallet connector
- add network-specific deployment config
- add subgraph or indexing support
